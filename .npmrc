# Ne pas faire échouer npm install sur les vulnérabilités "moderate" de devDependencies.
# Les 10 vulnérabilités ajv/ESLint sont connues : dev-only, production = 0 vuln.
# Voir : npm run audit:prod
audit-level=high
