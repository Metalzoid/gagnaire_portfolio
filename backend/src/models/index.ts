/**
 * Couche modèle / repository.
 * Les modèles de données sont définis dans Prisma (prisma/schema.prisma).
 * Ce module réexporte le client Prisma et peut contenir des repositories
 * ou helpers spécifiques par entité si besoin.
 */
export { prisma } from "../config/database.js";
